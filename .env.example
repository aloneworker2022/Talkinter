# Copy to .env and adjust, then:  set -a && . ./.env && set +a && npm start

# ---- Server ----
HOST=0.0.0.0
PORT=8787
# Optional shared password. If set, the web UI asks for it once.
# AUTH_TOKEN=change-me

# ---- Choose how Talkinter reaches your agent ----
# mock | command | http
AGENT_ADAPTER=mock

# ---- command adapter: wrap your Hermes CLI (replaces the SSH terminal) ----
# Use {message} as a placeholder, or omit it to send the message via stdin.
# AGENT_ADAPTER=command
# AGENT_CMD=hermes chat --prompt {message}
# AGENT_CMD=hermes                 # message piped to stdin
# AGENT_CMD_MODE=oneshot           # oneshot | persistent
# AGENT_CMD_IDLE_MS=700            # persistent mode: ms of silence = reply done
# AGENT_CMD_CWD=/path/to/hermes

# ---- http adapter: any OpenAI-compatible /v1/chat/completions endpoint ----
# AGENT_ADAPTER=http
# AGENT_HTTP_URL=http://localhost:11434/v1/chat/completions
# AGENT_HTTP_KEY=sk-...
# AGENT_HTTP_MODEL=gpt-3.5-turbo
# AGENT_HTTP_SYSTEM=You are Hermes, a helpful assistant.

# ---- Branding ----
APP_TITLE=Talkinter
APP_SUBTITLE=Hermes agent
